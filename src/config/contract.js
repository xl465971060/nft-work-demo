import marketplaceData from "../Marketplace.json";

// 合约地址与 ABI（deploy 脚本会自动刷新 Marketplace.json）
export const contractAddress = marketplaceData.address;
export const contractABI = marketplaceData.abi;
